@model IEnumerable<GuardaCultura.Models.EstacaoAno>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome_estacao)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome_estacao)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EstacaoAnoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EstacaoAnoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EstacaoAnoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
